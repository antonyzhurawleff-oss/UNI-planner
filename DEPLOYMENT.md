# Инструкция по развертыванию проекта

Это руководство поможет вам развернуть проект онлайн и поделиться им с другими.

## Вариант 1: Vercel (Рекомендуется - самый простой)

Vercel создан командой Next.js и обеспечивает самое простое развертывание.

### Шаг 1: Сохраните проект в GitHub

1. Создайте аккаунт на [GitHub](https://github.com), если его нет
2. Создайте новый репозиторий:
   - Перейдите на https://github.com/new
   - Название: `admissions-planner-mvp`
   - Выберите **Private** (если не хотите делиться кодом)
   - Нажмите "Create repository"

3. Сохраните проект в GitHub:

```bash
# Если Git еще не инициализирован
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit"

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/admissions-planner-mvp.git

# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

### Шаг 2: Разверните на Vercel

1. Перейдите на [Vercel](https://vercel.com)
2. Войдите через GitHub аккаунт
3. Нажмите **"Add New Project"**
4. Выберите репозиторий `admissions-planner-mvp`
5. В настройках проекта добавьте **Environment Variables**:

   - `OPENAI_API_KEY` = `ваш_openai_ключ`
   - `SERPAPI_KEY` = `ваш_serpapi_ключ`

6. Нажмите **"Deploy"**

7. Готово! Vercel предоставит вам URL, например: `https://admissions-planner-mvp.vercel.app`

### Важные замечания для Vercel:

⚠️ **Хранение данных**: Файл `data/submissions.json` будет сбрасываться при каждом перезапуске сервера в бесплатном плане Vercel. Для продакшена рекомендуется использовать базу данных (например, PostgreSQL через Vercel Postgres).

## Вариант 2: Netlify

1. Зарегистрируйтесь на [Netlify](https://www.netlify.com)
2. Подключите ваш GitHub репозиторий
3. Настройки сборки:
   - Build command: `npm run build`
   - Publish directory: `.next`
4. Добавьте Environment Variables в настройках проекта
5. Deploy

## Вариант 3: Railway

1. Зарегистрируйтесь на [Railway](https://railway.app)
2. Создайте новый проект из GitHub репозитория
3. Добавьте Environment Variables
4. Railway автоматически определит Next.js и развернет проект

## После развертывания

### Обновите README.md

Добавьте в README.md ссылку на ваш живой проект:

```markdown
## 🚀 Live Demo

Доступен по адресу: https://your-project.vercel.app
```

### Что нужно знать:

1. **API ключи**: Никогда не коммитьте `.env.local` в Git - он уже в `.gitignore`
2. **Переменные окружения**: Всегда добавляйте их через интерфейс платформы развертывания
3. **Обновления**: После каждого `git push` в основной ветку проект автоматически обновится на Vercel/Netlify

## Быстрый старт для Vercel (через CLI)

Если хотите использовать командную строку:

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в аккаунт
vercel login

# Разверните проект
vercel

# Следуйте инструкциям в терминале
```

## Поддержка

Если возникнут проблемы:
- Vercel документация: https://vercel.com/docs
- Next.js документация: https://nextjs.org/docs
